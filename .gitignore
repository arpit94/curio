# Environment
.env
.env.local

# uv virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Data (keep dir, ignore contents)
data/*.db
data/*.sqlite

# Logs (fully ignored)
logs/

# Editions are the emails themselves — the actual send is the source of truth;
# archived .md / .html are useful locally (weekly Sunday review reads them)
# but shouldn't churn git history.
editions/

# Proposed config edits from Sunday review — human reviews then merges to taste.yaml
config/taste.yaml.proposed

# OS files
.DS_Store
